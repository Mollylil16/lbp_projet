/**
 * Composant principal de l'application
 * Utilise lazy loading pour réduire le bundle initial
 */
import { AppRoutes } from './routes'

function App() {
  return <AppRoutes />
}

export default App
